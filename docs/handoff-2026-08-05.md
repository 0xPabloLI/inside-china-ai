# Session Handoff — 2026-08-05

## 本 Session 完成的工作

### F5-TTS-MLX 修复
- `duration = ref_dur + target_dur`（之前不设导致 0.03s 音频）
- ref-text 精确匹配 ref-audio（之前用 Whisper 识别导致废话泄漏）
- 跳过 silenceremove（F5 音频振幅低，会被 -35dB 阈值全删）
- `f5_mlx_batch_tts.py` — batch TTS 脚本
- `text-align.py` — wav2vec2 forced alignment（用原始文字，不通过语音识别）

### 字幕系统重构
- **渲染方式**：CSS @keyframes → JS requestAnimationFrame → **FFmpeg ASS 原生烧录**（最终方案）
- **字体大小**：libass 默认 PlayResY=288 导致 FontSize=42 放大 6.7 倍 → ASS 文件显式设置 `PlayResX=1080, PlayResY=1920`
- **位置**：MarginV=200 与 TikTok 底部 UI 重叠 → **MarginV=450**（避开用户名/按钮/描述区）
- **时间**：wav2vec2 时间戳偏晚 → **-0.3s 偏移**（字幕先于语音出现）
- **工具**：安装 `ffmpeg-full`（含 `--enable-libass`），路径 `/opt/homebrew/opt/ffmpeg-full/bin/ffmpeg`
- **文档**：`docs/brand-system.md` 加了 Subtitle Specification 章节

### 多 Pipeline 隔离架构
- `content/{article}/` 目录 — 每篇文章独立目录（meta.mjs + scene-data.mjs + scenes.mjs）
- `output/{pipelineId}/` — 输出隔离，多 pipeline 并行不冲突
- `main.mjs` — `--content` 参数动态加载 content 目录
- `generate-scenes.mjs` — visualType 注册表，按 `visualType` 分发（不按 sceneId）
- scene data 从 git HEAD 恢复原始 DeepSeek 内容（根目录 scene-data.mjs 被其他 session 改成蒸馏 pt3）

### AGENTS.md
- 加了 M4A → WAV 转换指南
- 加了 Playwright 对齐验证规则

## 当前产出

output/ 目录已清空（历史中间产物已删除）。用户后续可根据 source material 重新跑 pipeline 生成视频。

### 已清理的历史文件
- 7 个根目录 scene-data*.mjs（被 content/ 目录结构取代）
- whisper-align.py（被 text-align.py + force-align.py 取代）
- assemble-only.mjs, run-whisper.mjs（import 路径已 broken）
- 14 个实验/测试 Python 脚本（test-*.py, analyze-*.py 等）
- fetch-best-practices*.mjs, tts-test.mjs, view-logo.mjs, tts-config-log.md
- output/ 下所有生成产物（视频、音频、HTML、预览图）

## 后续待做

### 1. ~~完全提取 s1-s12 到 content/deepseek/scenes.mjs~~ ✅ DONE (2026-08-05)
**完成**：s1-s12 已提取到 `content/deepseek/scenes.mjs`，共享视觉系统在 `lib/base-styles.mjs`。`generate-scenes.mjs` 已删除。

### 2. ~~场景模板参数化（读 scene.texts 而非硬编码）~~ ✅ DONE (2026-08-05)
**完成**：所有 12 个场景函数接收 `(scene, duration)`，从 `scene.texts` 读取显示文字。`scene-data.mjs` 的 `texts` 字段已补全。缺失字段安全降级。

### 3. ~~蒸馏系列专用视觉模板~~ ✅ DONE (2026-08-05)
**完成**：`content/distillation/pt1/scenes.mjs` 已实现 8 个独特视觉场景：
- S1 hook: 数据泄露警报风格（红色 glitch 特效 + stamp-in 动画）
- S2 contrast: 表面 vs 深层窃取（左红删除线 / 右琥珀高亮）
- S3 timeline: 破解序列流程图（终端风格 + 步骤箭头）
- S4 data-table: Anthropic 指控数据表（条形图可视化 + 最大值红色高亮）
- S5 quote: 加密博客引用确认（大引号 + 绿色验证徽章）
- S6 contrast: 被点名 vs 未点名（红/琥珀双列）
- S7 teaser: Part 2 预告（琥珀色大字 + 分隔线）
- S8 cta: 品牌结尾（logo + 品牌名 + fade-to-black）
- 视觉 DNA：红色主导 + glitch 特效 + 终端美学（区别于 DeepSeek 的蓝色/琥珀）
- 65 个单元测试覆盖所有场景
- pt2/pt3 仍为 stub（后续按需实现）

### 4. ~~基础设施文件移到 lib/~~ ✅ DONE (2026-08-05)
**完成**：generate-tts, assemble, generate-srt, record-scenes, generate-bgm 全部移到 `lib/`。`__dirname` 路径已修复（用 `ROOT_DIR`）。`assemble.mjs` 输出文件名 bug 和 `generate-bgm.mjs` 输出路径 bug 已修复。

### 5. ~~verify-subtitles.mjs 集成~~ ✅ DONE (2026-08-05)
**完成**：`verify-subtitles.mjs` 重写为 CLI wrapper，委托给 `lib/verify-subtitles.mjs` 模块。语法 bug (`toFixed(1f)`) 已消除。pipeline Step 6 自动运行验证（`--skip-verify` 跳过）。输出 JSON 报告到 `output/{pipelineId}/verification-report.json` + console summary。

### 6. ~~自动化测试~~ ✅ DONE (2026-08-05)
**完成**：
- FFmpeg `silencedetect` 对比字幕时间戳（音频静默段 vs 字幕空隙）
- 同步偏差检测（字幕 start vs silence_end，>0.5s = deviation）
- JSON 同步报告（覆盖率%、间隙列表、过短字幕、同步偏差）
- 27 个单元测试覆盖场景矩阵（空输入、边界值、解析容错）
- 不依赖 Python，纯 Node.js + FFmpeg

## 关键文件位置

| 文件 | 用途 |
|------|------|
| `scripts/short-video/content/deepseek/scene-data.mjs` | DeepSeek 12 场景数据（texts 字段已补全） |
| `scripts/short-video/content/deepseek/scenes.mjs` | DeepSeek 12 个独特视觉场景（从 scene.texts 读取） |
| `scripts/short-video/content/deepseek/meta.mjs` | `{ pipelineId: "deepseek" }` |
| `scripts/short-video/content/distillation/pt1/scenes.mjs` | 蒸馏 pt1 — 8 个独特视觉场景（数据泄露风格） |
| `scripts/short-video/content/distillation/pt1/scene-data.mjs` | 蒸馏 pt1 场景数据（8 场景 + seriesMeta） |
| `scripts/short-video/content/distillation/pt1/meta.mjs` | 蒸馏 pt1 元数据 |
| `scripts/short-video/content/distillation/pt2/` | 蒸馏 pt2（scene-data + meta，scenes 仍为 stub） |
| `scripts/short-video/content/distillation/pt3/` | 蒸馏 pt3（scene-data + meta，scenes 仍为 stub） |
| `scripts/short-video/lib/base-styles.mjs` | 共享视觉系统（CSS 变量、背景层、动画、品牌 SVG、UI 积木） |
| `scripts/short-video/lib/generate-tts.mjs` | TTS 引擎（F5/XTTS/Kokoro/edge-tts/say） |
| `scripts/short-video/lib/generate-srt.mjs` | ASS 生成（PlayResX=1080/PlayResY=1920, MarginV=450, -0.3s offset） |
| `scripts/short-video/lib/assemble.mjs` | FFmpeg 拼接 + ASS 字幕烧录（用 ffmpeg-full，输出用 pipelineId 命名） |
| `scripts/short-video/lib/record-scenes.mjs` | Playwright 录制 |
| `scripts/short-video/lib/generate-bgm.mjs` | BGM 生成（输出到管线隔离目录） |
| `scripts/short-video/main.mjs` | --content 参数 + 输出隔离 |
| `scripts/short-video/text-align.py` | wav2vec2 forced alignment |
| `scripts/short-video/f5_mlx_batch_tts.py` | F5-TTS-MLX batch 生成 |
| `scripts/short-video/verify-subtitles.mjs` | CLI wrapper（字幕覆盖率 + 同步验证，委托 lib 模块） |
| `scripts/short-video/lib/verify-subtitles.mjs` | 验证核心模块（纯函数 + FFmpeg silencedetect + JSON 报告） |
| `docs/spec-subtitle-verification.md` | 验证系统 spec（含场景矩阵） |
| `docs/tickets-subtitle-verification.md` | 验证系统实施 tickets |
| `scripts/short-video/assets/logos/` | 公司 logo 库（deepseek.svg，未来添加更多） |
| `docs/brand-system.md` | 字幕规范（Subtitle Specification 章节） |
| `docs/spec-pipeline-isolation.md` | 多 Pipeline 隔离架构 spec |
| `docs/spec-scene-extraction.md` | 场景提取 + 共享视觉系统分离 spec |
| `docs/tickets-scene-extraction.md` | 场景提取实施 tickets |

## 运行方式

```bash
# DeepSeek 视频
node scripts/short-video/main.mjs --content deepseek --bgm

# 蒸馏 pt1
node scripts/short-video/main.mjs --content distillation/pt1 --bgm

# 默认（不加 --content = deepseek）
node scripts/short-video/main.mjs --bgm
```

## Git Commits（本 Session）

```
e7c7e8e feat(templates): visualType-based dispatch
47a8a28 feat(arch): multi-pipeline isolation — content/ + lib/ structure
2adff18 fix: ASS file with PlayResX=1080 PlayResY=1920
41abf2e fix: MarginV=450 — above TikTok bottom UI zone
c3bd612 fix: subtitle timing -0.3s offset + FontSize=42 MarginV=200
11493c2 fix: use ffmpeg-full for subtitle burning (has libass)
95226cf fix: use execFileSync for subtitle burning
e035733 feat(subtitles): native FFmpeg subtitle burning via SRT
caaf823 fix(subtitles): remove per-word karaoke highlighting
73c40a2 feat(subtitles): JS requestAnimationFrame rendering
3391583 fix: subtitle gap-fill + M4A handling docs
6b93fe2 fix: F5 duration = ref_dur + target_dur
d710557 feat(verify): subtitle verification system — coverage + sync + duration checks
```

## 注意事项

1. **ffmpeg-full 路径**：`/opt/homebrew/opt/ffmpeg-full/bin/ffmpeg`（含 libass）。普通 ffmpeg 没有 subtitles filter。
2. **F5 venv**：`~/.f5-tts-env`（Python 3.11），whisperx 也装在这里。
3. **根目录旧文件已清理**：scene-data*.mjs、whisper-align.py、实验脚本等已删除。所有 scene-data 在 `content/` 目录下。
4. **GitHub 网络**：push 偶尔超时，重试即可。
5. **AGENTS.md Session Boundary**：只 commit 本 session 改动，其他 session 的用 `git add -p` 选择性提交。
