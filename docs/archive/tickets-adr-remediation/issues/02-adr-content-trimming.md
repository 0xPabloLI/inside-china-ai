# 02 — Trim ADR content to ADR-FORMAT compliance (0007, 0008, 0009, 0010, 0011, 0012, 0013, 0015)

**What to build:** Strip L1/L2 content and stale data from 8 ADRs, leaving each as 1-3 sentences (decision + why) + optional Considered Options (1-2 sentences per alternative) + optional Consequences (1-2 sentences with pointers to L1/L2 where detail lives). Delete all parameter tables, API surfaces, performance tables, bug fix logs, implementation status tracking, and evaluation comparison tables.

**Blocked by:** 01 (video-workflow.md VLM section must exist before ADR-0009/0015 Consequences can point to it).

**Status:** ready-for-agent

- [ ] ADR-0007: 6 decisions each trimmed to 2-3 sentences. Delete "Why full rebuild" rationale (stale — incremental implemented). Delete Stage 5a trigger list (already in content-pipeline.md). Add Consequence: "Incremental indexing implemented (commit ea20bd3). See `content-pipeline.md` Stage 2c/3b/4b."
- [ ] ADR-0008: Keep decision (F5-TTS-MLX default) + 1-sentence trade-off + Consequences with pointers to `video-workflow.md` TTS section and `voice-cloning-solutions-m2-pro.md`. Delete: parameter table, post-processing rules, bug fixes, evaluation comparison, trade-offs table, registry priority, venv path, subtitle alignment.
- [ ] ADR-0009: Keep decision (Qwen3-VL-8B via mlx-vlm) + 1-sentence trade-off + Consequences with pointers to `video-workflow.md` VLM section and `asset-focus-detection-alternatives.md`. Delete: architecture diagram, API surface, performance table, key technical decisions, trade-offs table, known limitations (all migrating to video-workflow.md), venv/model paths.
- [ ] ADR-0010: Keep decision (Remotion replaces Playwright) + 1-sentence alternatives (FFmpeg-only rejected for text layout, Playwright rejected for frame accuracy, Motion Canvas rejected for ecosystem) + Consequences with dual-track note. Delete: migration scope table (T1-T6), scene component table, CSS-to-Remotion mapping, trade-offs table, project path.
- [ ] ADR-0011: Keep decision (unified venv ~/.video-tts-env) + 1-sentence problems (separate venvs caused disk waste, PATH conflicts, version fragmentation) + Consequences. Delete: component table, Python version rationale, old venv deletion note, text-align.py path, whisperx upgrade note.
- [ ] ADR-0012: Keep decision (Kaggle + Colab primary) + 1-sentence alternatives + Consequences with pointer to `cloud-gpu-options.md`. Fix stale: Lightning AI now usable (15 credits/month, not 403), `machine_shape` works (not "cannot be specified"). Delete: Kaggle config details, Colab config details, P100 vs T4, provider evaluation table, scripts path.
- [ ] ADR-0013: Keep decision (three-layer collection with per-source fallback) + 1-sentence alternatives + Consequences with pointer to `content-pipeline.md`. Delete: architecture diagram, source registry fields, 28-source classification table, paidApi flag, search-sources.mjs entry point, collectFromSource() order, Issue #33/#34 status.
- [ ] ADR-0015: Keep decision (OpenCV subprocess for focus detection) + 1-sentence alternatives (VLM-only rejected for instability, YOLO/SAM rejected for weight, Cloud rejected for cost/privacy, OpenCV 5.0 rejected for removed modules) + Consequences with pointer to `video-workflow.md` VLM section and note about 4.10.0.84 pin. Delete: architecture diagram, two-phase execution, API surface, output contract, 6 key technical decisions, performance table, dependencies table, Phase 2 consumers, test count, relationship section.
