# Issue Tracker 轮换历史（2026-09-06 下沉归档）

> 本文为 `docs/issue-roadmap.md` 头部轮换历史（第十八 session 及更早的全部 Last inventory / Previous 轮次）的归档快照，2026-09-06 由第十八 session 文档梳理从 tracker 单行（51,686 字符）下沉。当前轮次见 tracker 头部；Closed Issues 明细表仍在 tracker 正文。

Last inventory: 2026-09-06（第十八 session）— **#77 ✅ 交付并关闭**（GitHub #77 CLOSED；审计报告 `docs/research/source-capabilities-audit.md`，取代 2026-08-19 首轮报告覆盖面）：64 源 schema 时代全面复核——**总体对齐**（articles 全量 enrichWithCapabilities 推导、stock 显式、CDP/YTDLP 映射合并；yt-dlp 白名单三处一致，xhs/douyin/weibo_hot 误标无残留）。**11 疑点**：P1 baidu_news 缺 CDP_MEDIA_CAPABILITIES entry（区块注释/notes/articleScript 提取 imageUrl 三处自相矛盾，修复归 #75）；P2 yt-dlp 白名单双份维护（registry 导出 `SUPPORTED_YTDLP_PLATFORMS` 零生产消费者，真正 gate 在 asset-sourcer 私有 Set）、`AUTOGEN_EXCLUDED_SOURCES` typo `pexels_video`（实际 `pexels-video`，死条目）、图片引擎池游离 registry 外（prog-search 四引擎无条目 + brave_image/searxng_image 双份定义——建议单独立票）；P3 articles 级 requiresApiKey/apiKeyEnv 死字段 + authRequired 无运行时 gate（gnews/currents 空 key 401）、mcp_grok_search 空 URL 空跑 CDP、douyin autogen site: fallback 恒空、searxng_search 静态绑定（#92 已知决策）、wechat2rss 空 articleScript（行为正确）、locale 风格、x_search apiFallback.type 死字段。**Fallback 链核验**：collectFromSource 六层实现与 registry 头注释一致，无「配置了但不走」的层；9 文章源零 fallback = AUTOGEN_EXCLUDED_SOURCES 全集（聚合器自为兜底，observation 非 bug）。**Video 调研定论（→#75 交接）**：应补录 baidu_news（随 P1 修复）+ xhs/douyin/weibo_hot（与下载器集成同 PR 防重蹈 P4 误标）+ tiktok_creator（先查 ScrapeCreators 字段）；live 抽样不标注 guancha（4 篇 0 video 标签）/36kr（视频在独立频道页）；聚合/学术/社交/新闻 API/wechat2rss/知乎等结构性不标注（全表报告 §4.3）。**测试缺口 3 项**（实施归 #75 或独立票）：collectFromSource 全链降级集成测试、CDP_MEDIA_CAPABILITIES 完整性守卫、零 fallback 源清单锁定。**② #68 ✅ 交付并关闭**（GitHub #68 CLOSED；审计报告 `docs/reviews/signal-density-audit-2026-09-06.md`）：ADR-0016 Rule 2 全管线审计——**设计时调用点全面达标**（5 条 Already applied 全部成立，VLM 实际 8 信号；#189/#100/#98/#99/#101 的缓存族已覆盖大部分昂贵调用）。**残留违规 6 项 → #198 follow-up**（render 后处理 4-pass ffmpeg 合并 > TTS/alignment 跨 run 缓存 > VLM crop hint 断层（simulate_crop 固定中心裁切 vs Phase 2 saliency 偏移裁切坐标系错位）> b-roll gate VLM 绕过 vlm-cache > verify-frames 逐帧从头解码 > search-sources↔asset-sourcer 同搜索页双开）；良性记录（3× ffprobe、escalation 抽帧、verifyWithRetry、fetch-page 死模块、temporal-focus/asr 未接线）。**③ #76 ✅ 交付并关闭**（GitHub #76 CLOSED；审计报告 `docs/reviews/ssot-audit-2026-09-06.md`）：SSOT 违规全管线审计——开票 5 项违规 **4 项已解决/缓解**（scene-data texts 由 REMOTION_SLOT_MAP+assertKnownTextFields 双侧运行时强制、search-results-cache 读写同源单模块、media 对象 types.ts 镜像+validateMedia preflight、discovery.json 版本化 schema）。**新发现 2 项高影响 → #199**：NO_MEDIA_TYPES 双份（scene-rules.mjs:1063 私有复制 claim-keywords.mjs:19 的 SSOT，1 行修复）、validateDiscovery 无生产消费者（discovery schema 只有测试在守）。低影响 7 类同记 #199（CDP 候选 JSDoc 漂移实锤、trending-topics 读写独立拼装、3 产物文件无 schemaVersion、cap?.x ?? source.x 双读 14 处、KNOWN_COMPANIES 双份不一致、MediaField 枚举 TS/.mjs 双写、tts/types.mjs 名不副实）。**TODO ①（lib/types.mjs）判断不做**：.mjs 侧有效约束机制是「共享常量+运行时校验器+preflight」，types.ts 无 .mjs 消费方，再建并行类型收益低（判断依据落报告 §0）。**TODO ③ 本票内交付**：DOCS-INDEX adr/ 段新增 Architecture Principles 表（Cascade/Signal Density/SSOT/SVE × ADR/归档/issue）。**④ #87 ✅ 交付并关闭**（GitHub #87 CLOSED；审计报告 `docs/reviews/maintenance-audit-2026-09-06.md`）：88 项手工维护面 64 源时代重新盘点（实测 ~96 项）——开票 5 类「坏了没人知道」**4 类已翻绿**（extractScript 五层兜底链完整/googleSiteFallback autogen 15 源/apiSearch.parser 抛错自动落 CDP/enrichWithMedia 接管；primaryScript 并入 CDP_MEDIA_CAPABILITIES）。**唯一仍成立原始痛点：静默 0 结果**（选择器腐烂返回 [] 不进 failedSources，:687 只收 throw；loginCheck 断裂同样静默）→ **#200**（纯可观测性：per-source 层级轨迹 + 零结果汇总）。#66 的 Jina/generic-eval 层未接（fetch-page 死模块沿 #187 先例记录），但被 autogen+pool+MCP 稀释，剩余价值即 0 结果检测。新增维护面 5 项（CDP_VIDEO_SCRIPT 共用、IMAGE_SEARCH_ENGINES 游离 registry、parseYtdlpSearchOutput 版本敏感、SITE_RATE_CONFIG、SearXNG theme）大半已在 #77 在案；判断「可接受人工」为主。**W3 审计类（#68/#76/#77/#87）全部清空**。**下一 session 候选**：① #75 capabilities.videos 补录 + 下载器集成（#77 基线已就绪；asset-sourcer/source-registry 大票）；② #198 Signal Density follow-up（render/TTS 提速直接改善生产效率）；③ #199 SSOT follow-up（NO_MEDIA_TYPES 1 行修复优先）；④ #200 source health log（纯可观测性小票）；⑤ #155 aiImage T2I（tools-catalog 准入 + GPU Kaggle 优先）；⑥ #194/#179/#35 暂缓不变。Previous: 2026-09-06（第十七 session）— **① #197 ✅ 交付并关闭**（commit `bec4e25` 在本地 main，未推送——push 待用户授权；GitHub #197 CLOSED）：安全扫描层去闭源——**替代组合**：Semgrep CE（SAST，p/default 213 规则；mimosa 深扫引擎本就是 semgrep 1.136.0，行为对齐）+ Cisco mcp-scanner（Agent/MCP 配置，YARA+prompt_defense 纯本地零 API key；snyk/agent-scan 因必需 SNYK_TOKEN 云分析被否决，Opengrep 留作 Semgrep 规则许可不可接受时的退路）+ osv-scanner（supply-chain+license，Apache-2.0）+ gitleaks（已有）。**npm scripts 一键入口**：`scan`（=code+deps）/`scan:code`/`scan:code:all`（scripts/ 管线 231 findings 噪声，advisory 单列不进默认）/`scan:mcp`/`scan:deps`。**smoke 证据**：osv-scanner 首扫 **5 真实 High 漏洞**（brace-expansion/js-yaml/nanoid，dev 依赖传递，全部有修复版——依赖升级待办）；semgrep src/ 3 findings 全为 test 文件 path-traversal audit FP（triage 记录在研究报告）；mcp-scanner stdio 直连验证通过（context7 2 tools HIGH 为已知激进启发式），config 模式本环境连接超时（上游 `--stdio-timeout` 标志另有 UnboundLocalError bug，绕行 `MCP_SCANNER_STDIO_TIMEOUT` 环境变量）→ `scan` 聚合入口不含 mcp。**附带修复**：`.mcp.json` 固定 `@upstash/context7-mcp@4.0.5`（mimosa agent-check 的 supply-chain 发现）。**退役清理**：`.mimosa/` 504MB + `~/.mimosa/` 24KB 删除；mimosa 本体为 ZCode harness 插件，harness 侧卸载。**hooks 决策**：不进 pre-commit/pre-push 阻塞路径（5 个未修漏洞在，push 即 block 破坏工作流）；CI SARIF N/A（仓库无 CI workflow）。研究报告 + 功能覆盖对比表 + 准入提案：`docs/research/security-scanner-oss-replacement-research.md`。R1 定级：diff 自审 + 定向验证，未跑正式 code-review（纯新增检查层，3 文件 +148 行，不触管线）。**② #186 ✅ 关闭**（用户授权 agent 裁决）：结构性视频素材稀缺——裁决 **image-only 为管线合法默认输出**（didi-robotaxi-r2 实证交付）；最高产量增量源（抖音/微博官方号）归 #75 第二批 adapter；图生视频归 #155/#157；licensed stock 延后不引入；close comment 落 GitHub。**下一 session 候选**：① **#77 源标注审计 → #75 capabilities.videos 补录**（审计先行，#75 依赖 #77 基线）；② **#155 aiImage T2I**（backdrop 自然下游，在线服务需走 tools-catalog 准入 + GPU Kaggle 优先规则）；③ W3 审计 #68/#76/#87；④ #194/#179/#35 暂缓不变（等独立 session）。**追加（第十七 session 继续，用户批准）**：osv-scanner 首扫的 5 个依赖 High CVE 已清零（`48004ee`，lockfile 12 行 patch 升级，全量 3091 tests 绿）；MediaBackground 陈旧契约对齐 #156 设计（`c90e249`，HEAD 既有失败非依赖引起）；pre-push 扫描 gate（`022aa6e`，osv-scanner+semgrep src/ 阻塞、工具缺失优雅跳过、scan:mcp 保持手动；.githooks/ 自 f394181 被 ignore 但共享 hook 均跟踪中，pre-push 显式纳管）。#75 已增补公司级泛搜 + rednote-mcp 视频下载优先级（GitHub comment）。Previous: 2026-09-05（第十六 session）— **① #92 ✅ 交付并关闭**（commit `724151f` 在本地 main，未推送——push 待用户授权；GitHub #92 CLOSED）：SearXNG 自托管 metasearch 源接入——`searxng_search`（GENERAL_SEARCH_SOURCES，#64 后第 64 源）apiSearch 直连 localhost:8888 JSON API（collectFromSource Layer 0；parser 截 20 条 + 滤无 url 结果）+ CDP HTML 结果页兜底（articleScript：`article.result`/`h3 a`/`p.content`）；enrichWithCapabilities 自动产出 capabilities.articles（method=api、无 googleSiteFallback——#88 API 源豁免规则）；rate-limiter 加 `localhost` 条目（零延迟、无小时上限——自托管前端不设限，仅作用于 CDP 兜底路径）；SOURCE_ATTRIBUTIONS 补 searxng_search（issue 要求的 SOURCE_LICENSES 全仓不存在；license 记 "Varies" 而非 AGPL-3.0——AGPL 是 SearXNG 代码自身许可，聚合结果版权异构，与 duckduckgo/google 同规，显式决策记入 issue）。**环境修复（开工前置）**：JSON API 403 双根因——容器 settings.yml 被回退丢 `search.formats: [html,json]`（已恢复）+ SearXNG httpx 不读代理环境变量（settings.yml `outgoing.proxies` 指宿主代理）；另宿主代理端口 7890→7897，容器已带新 env 重建。验证：TDD 13 新项 red→green + 受影响 5 文件 236/236；真实冒烟 collectFromSource('DeepSeek R1') → **1.7s 20 条**（Reuters/Nature/Britannica 真实 URL）；全量 2436 绿，51 失败均为已知 worktree 环境类（Chromium render `npm error could not determine executable`、gitignored 媒体、#189 pool 抖动），主 checkout 同五文件 baseline 仅 1 已知抖动；lint/prettier 干净；code-review 两轴（88e2f87...724151f）：Standards 0 阻塞（全部既有 judgement call），Spec 4 项 finding 已处理（capabilities 测试陈旧计数注释随 amend 修复；其余决策随 close comment 落 GitHub issue）。**worktree LFS 事故记录**：本 session worktree 内 `git lfs pull` 把 20 个媒体文件覆盖为与 HEAD 不同的 LFS 对象版本，已按 HEAD blob 原样恢复并逐一 md5 核对——仓库存在「LFS 标记路径提交了原始二进制」既有不一致（git-lfs 报 "should have been pointers, but weren't"），后续 session 勿在 worktree 跑 `git lfs pull`。**② #100 ✅ 交付并关闭**（commit `6aec928` 在本地 main，未推送；GitHub #100 CLOSED）：P7 统一分析 envelope + focus 结果缓存——① vlm-cache v2：条目升级为 `{ok, data, error, meta}`（meta 记 model/durationMs/escalated/sourceMode/window/双版本/generatedAt；`VLM_CACHE_PIPELINE_VERSION` 进 cache key；legacy 裸条目按 miss 处理一次性失效；`writeCachedResult` 拒写非 ok 结果——失败永不成为成功命中）；② 新增 `lib/focus-cache.mjs`：焦点检测内容寻址缓存（SHA-256 源指纹 + FOCUS_ANALYZER_VERSION，同 envelope；degraded 瞬态结果在缓存层拒写，rerun 重试）；③ asset-sourcer Phase 2/3a 接线 + 命中日志带 model/时间戳；④ 排程不改码：pool（VLM_CONCURRENCY 默认 2，#189 实测 3.0x——偏离 issue「默认 1 heavy worker」的显式决策）+ RAM 门控升级（#127 check_ram）已可配置可观测。**显式决策**：analysisProfile 概念在代码库不存在，由 promptVersion+model+pipelineVersion 覆盖（记入 issue）；ASR/timeline 缓存无消费方不投机建（#187 先例，P5/P6 落地后接入）；FOCUS_CACHE_DISABLED 与 VLM_CACHE_DISABLED 对称。验证：TDD 14 新项 red→green（envelope 7 + focus 7）+ 受影响 270/270；**真实冒烟**：focus 6779ms→1ms 命中（3468×4624 大图，实际成本比 T06 注释的 0.5s 高一个量级）；VLM Run1 132.2s（含 GLM-9B 升级）→ Run2 **0.0s** 双缓存命中、描述逐字一致；全量 2450 绿，51 失败与基线同模式（worktree 环境类）；code-review 两轴（d3263f7...6aec928）：Standards/Spec 共 7 项 finding——4 项随 amend 修复（meta 补 window、degraded 拒写下沉、守卫顺序、误导注释），3 项记 issue（analysisProfile 覆盖决策、unsupported 可缓存论证、大文件指纹为 #189 既有设计）。**③ #101 ✅ 交付并关闭**（commit `78eb536` 在本地 main，未推送——经 cherry-pick --no-commit 从 session 分支 `ee9f947` 落库，避开与并行 session `1f195cb` 的非 ff 分叉；GitHub #101 CLOSED）：P8b temporal focus——新增 `lib/temporal-focus.mjs`：① `analyzeTemporalFocus(videoPath, {startMs, endMs, samplingProfile, cacheDir, deps})` 窗口内均匀抽 3–5 帧（默认 4，边界含入，绝不只取 poster），默认接真实 Focus worker（deps 可注入），逐帧保留 timeMs/protectedRegions/saliency/降级状态；② 确定性聚合 `aggregateFrameEvidence`（纯函数）：stableProtectedRegions（跨帧 IoU≥0.4 聚类，仅 usable 帧投票，presenceRatio≥50%）、subjectMotion（saliency/区域质心最大位移 >0.1 → moving；safe-static / moving / none 三态 + 顶层 status 区分 degraded）、全低信息 → low_information；③ `transformedFocus(focus, {targetAspect, fit, cropPolicy})` 纯几何 cover/contain 映射到 9:16/16:9/1:1（center/saliency 锚定，crop 外区域 drop+上报），baseFocus 用 protectedRegions、temporalFocus 用 stableProtectedRegions（kind 判别）；④ 缓存按 #100 envelope 协议（指纹+窗口+sampling+TEMPORAL_FOCUS_VERSION，degraded 拒写）。schema 边界：base/poster/temporal 三种 focus 不互相覆盖；**不接 Remotion 模板**（issue 非目标）；transformedFocus 输出即未来 `resolveLayout` 的 focus 输入契约（#94）。验证：TDD 21 新项；真实冒烟 unitree-demo.mp4：真实 ffmpeg 抽帧 + 真实 OpenCV worker 5.0s（0–8s 机器人运动窗口判 moving、位移 0.16）→ 重跑 0.0s 缓存命中、逐字一致；transformedFocus 三目标画布全通过；全量 2471 绿 + 51 已知环境类失败；lint 干净；code-review 两轴（6aec928...ee9f947）13 项 finding——7 项随 amend 修复（worker 默认接线、死缓存调用、聚类仅 usable 帧、intersectExtent helper、key 常量注释、resolveLayout 契约记录、退化窗口测试），6 项记 issue（P4 窗口由调用方传入不重探、kind 判别仅 temporal 侧强制、base/poster kind 为约定、FFMPEG_FULL 第三份拷贝为既有模式、subjectMotion none 与 degraded 由 status 区分、大文件指纹沿 #189）。**④ #98 ✅ 交付并关闭**（commit `4fc28e6` 在本地 main，未推送——cherry-pick 自 session 分支 `5e5b8c9` 避开并行 session `9e9ba97` 非.ff 分叉；GitHub #98 CLOSED）：P5 本地 ASR worker——新增 `lib/asr_worker.py`（NDJSON IPC 常驻子进程，vlm_analyzer 同约定：requestId 回显、结构化错误 envelope、请求循环永不崩；whisperx/faster-whisper 懒加载，ASR_MODEL env 默认 base/本地 HF 缓存）+ `lib/asr-analyzer.mjs`（Node 网关 `transcribeAudioWindow(mediaPath, {startMs, endMs, languageHint})`：ffmpeg 窗口音频提取 16kHz mono、无音轨/提取失败分类降级、worker 相对时间戳偏移回媒体时间线并钳制进请求窗口、meta 全路径记 requestedWindow/backend/model/durationMs）+ 常驻单 worker（requestId 路由 + generation 隔离：超时杀 worker、迟到响应由代际检查丢弃——验收项）+ #100 envelope 缓存（指纹+窗口+语言+模型+ASR_VERSION，degraded 拒写）。**无 live 消费方**：P6 (#99) timeline fusion 是首个消费者，按设计不接 main.mjs（#187 先例）。验证：TDD 20 项（16 单测/IPC + fake-asr.py 夹具：requestId 路由、超时杀进程+迟到响应隔离、crash settle、错误 envelope、静音音频）；真实冒烟：英文 TTS wav 转写+缓存重跑 0.0s、窗口请求钳制在窗内；unitree-demo.mp4 中文音轨 → 句级毫秒时间码（base 模型中文质量一般——模型可 ASR_MODEL 升级，非模块问题）；全量 2487 绿 + 51 已知环境类失败；lint 干净；code-review 两轴（ba1fe30...5e5b8c9）9 项 finding——5 项随 amend 修复（durationMs 全路径、test seam 收紧删两分支、死字段、重复 import、静音测试），4 项记 issue（requestedWindow 为请求回显不重探、worker 机制与 visual-analyzer 第三份近拷贝——第四份触发提取、ASR_CACHE_DISABLED 良性、中文夹具为 fixture 级+真实冒烟补充）。**⑤ #99 ✅ 交付并关闭**（commit `f01b9d3` 在本地 main，未推送——cherry-pick 自 session 分支 `efd3be9`；GitHub #99 CLOSED）：P6 确定性媒体时间线融合——新增 `lib/media-timeline.mjs` `fuseMediaTimeline({visualWindows, asrSegments, mediaMeta}, {minEventMs})` 纯函数：边界扫描 → atomic membership 区间 → 同成员合并 → minEventMs（默认 200ms）以下边界抖动被吸收进邻事件（证据并集保留）；单模态事件永不丢失（无音轨→纯视觉时间线、纯音频→纯语音时间线）；事件携带 fused 标志 + evidence ids 溯源 + transcripts（id+text+language，可审计「哪段对白」）+ sourceMode 聚合（degraded 回退保留）+ min-confidence；全部时间钳制进媒体时长；**字节稳定**（无时间戳 + code-unit id 排序——localeCompare 环境依赖会破坏稳定契约，review 揪出修复）；5 个黄金 JSON fixture（fused-overlap / no-overlap / missing-audio / audio-only / window-fallback）。**无管线接线**（非目标）——下游消费者 #32 AI 片段选择与证据回放。验证：TDD 15 项；lint 干净；全量 2502 绿 + 51 已知环境类失败；code-review 两轴（4fc28e6...efd3be9）7 项 finding——4 项随 amend 修复（code-unit 排序、transcripts 进事件、minOverlapMs 改名 minEventMs 对齐 spec 语义、窗口回退专属 fixture），3 项记 issue（媒体元数据仅消费 durationMs、normalizeIntervals 联合形状为轻量妥协、输入顺序无关性由排序保证）。**⑥ #195 ✅ 交付并关闭**（commit `cfbcb95` 在本地 main，未推送——cherry-pick 自 session 分支 `ca020bf`；GitHub #195 CLOSED）：session-launcher stop 自动删除已合并 session branch——stop 从 pilot 状态文件读 session id（在清除前），worktree 移除成功后 `git branch -d`（绝不用 -D）：未合并分支被 git 安全拒绝并保留+原因输出，删除失败不阻塞 stop；dirty-kept worktree 不删 branch（工作未落库，branch 是唯一入口）。**真实场景验证**：20260904 三条存量 branch——两条已合并删除，`session/20260904-media-strategy-warn-193-7266f0` 被 `-d` 正确拒绝（持有等用户 WIP 的指南 commit `6a1e127`）。验证：TDD 3 分支用例（merged 删除 / unmerged 保留+原因 / dirty 保留）+ scripts/__tests__ 41/41；code-review（061f313...ca020bf）2 项 cosmetic finding 随 amend 修复（拒绝原因措辞覆盖 branch -d 其他失败面、state 缺失时不输出 session/unknown 误导行）。**同时创建 #196**（LFS 治理：20 个 LFS 标记路径提交了原始二进制，ready-for-human 待用户裁决方案 A/B）。**⑦ #191 ✅ 交付并关闭**（commit `4798719` 在本地 main——cherry-pick 自 session 分支 `6e55663`，已随 #195 批次推送；GitHub #191 CLOSED；用户裁决：按 agent 推荐全三步 + 顺序 #191→#192→#185）：mediaOptOut 退役——① `claim-keywords.mjs` 新增共享判定 `skipsMediaSourcing(scene)`（NO_MEDIA_TYPES → 显式 `media: null` → deprecated mediaOptOut → CSS-only layouts hero-center/stacked-cards），main.mjs scenesNeedingMedia 改用之（CSS-only 不再耗 sourcing 预算、media:null 跨 rerun 存活）；② final-media-gate：media-dependent layout 上的显式 media:null pass + `explicit-null-on-media-layout` WARN，legacy opt-out 仍 FAIL；`effectiveLayout` 导出供 scene-rules 复用；③ scene-rules 新增 `checkMediaOptOutDeprecation` 进 runAllSceneDataChecks（deprecation WARN + null-on-media-layout WARN，布局解析与 gate 一致——layout-less narrative 默认 media-bottom-bar 不漏报）；④ extractSceneClaims 按 spec 保持窄跳过（media:null 不跳 claim 提取）；⑤ types.ts `media?: MediaField | null` + mediaOptOut @deprecated；⑥ migration 全 14 处删除（kimi-ipo 9 / qwen4-preview 4 / _gate-smoke 1，全部 CSS-only 组合）。验证：TDD 11 新项 + 受影响 206/206；preflight 冒烟 kimi-ipo exit 0；合成场景 deprecation WARN 正常触发；全量 2514 绿 + 51 已知环境类失败；code-review 两轴（49a21df...6e55663）3 项 finding 随 amend 修复（checkMediaStrategyContract JSDoc 被挤占还原、extractSceneClaims 违 spec 收窄、effectiveLayout 一致性），2 项记录（NO_MEDIA_TYPES 单源 + final-media-gate re-export、optedOut 双 WARN 为过渡态噪声）。**⑧ #192 ✅ 交付并关闭**（commit `f3f50eb` 在本地 main——cherry-pick 自 session 分支 `9a74021`；GitHub #192 CLOSED；按已批准推荐设计）：per-scene asset 拒绝回路——① scene-data 顶层 `mediaReject: { reason?, rejected: string[] }`（URL 优先 + 本地 path 兜底；exact 恒匹配、basename 兜底仅限本地路径——CDN 同名 URL 不得误拒）；② asset-sourcer `isRejectedFor` 在三条分配路径（claim binding/hook/pass2）生效，claim binding 有专属 reason；被拒资产仍可分配给无 reject 的其他 scene；③ `apply-media-patch.mjs` 新增 `applyAssignedMedia` 纯函数（main.mjs Step 1.5c 提取）：不覆盖已有 media、缺文件跳过、**成功分配即清 mediaReject**、exhausted 上报（CSS-only 兜底）；④ 搜索缓存不失效（下游过滤）、VLM 缓存不绕过（同字节换 URL 罕见，rationale 落 types.ts 注释）；⑤ types.ts scene 级 mediaReject 声明。**真实两轮冒烟**（kimi-ipo，live 搜索 + 真实下载 + 真实 VLM 判分）：run 2（threshold 10）真实分配 `bing_images-moonshot-01.jpg` → scene 8；run 3 把该 path 写入 mediaReject 重跑 → 该资产被跳过，scene 8 改拿次优（真实 B站视频）。验证：TDD 10 新项（分配 5 + 应用 5）+ 受影响 285/285；全量 2524 绿 + 51 已知环境类失败；code-review 两轴（9154a4d...9a74021）7 项 finding——5 项随 amend 修复（JSDoc 归位、URL basename 收紧、模块头双职责、返回值冗余、exhausted 措辞 + VLM rationale 落文档），2 项记录（Step 1.5c 提取顺带让 existing-media 跳过可见化为 warning——行为变化已记录；exhausted 含「无可候选」场景的措辞已放宽）。**⑨ #185 ✅ 交付并关闭**（commit `7973bf3` 在本地 main——cherry-pick 自 session 分支 `1e412fc`，已推送；GitHub #185 CLOSED；按已批准推荐设计）：平台语言关键词路由——① `claim-keywords.mjs` 新增 `extractZhKeywords(text, {limit})`：标点切句 + CJK 2-4 字 n-gram 按频次（≥2）评分 + 停用字/拉丁数字过滤 + 贪心子串去重（同频先留长 gram）+ 确定性排序，无分词库（免 tool-admission）；`extractSceneClaims` 透传 `scene.sourceRef`；② `buildQueryGroups` 产出 `zhQueryGroups`（scene.sourceRef.sourceText 派生，绑 claimSceneId）；③ `mergeZhPools`（sourceRef 领先 + COMPANY_NAME_ZH 兜底去重，#180 保留）→ zh-CN 视频源搜合并池；国际图片/API 源把 zh 组**追加在 en 组之后**（引擎可处理 CJK；图片引擎按设计不加 locale 字段）；④ types.ts `sourceRef?: {url?, title?, sourceText?}`（scene 级可选字段，无需校验器改动）。**真实冒烟**：qwen4-preview scene 6 种入真实中文 sourceRef → zh 池 `[通义团队, 混合注意, 合注意力]`，live 搜索覆盖 B站（合并池）与 SearXNG/Bing/Google Images（追加组），真实分配落位。验证：TDD 10 新项 + 受影响 302/302 + 全量 2535 绿 + 50 已知环境类失败；lint 干净；code-review 两轴（b493536...1e412fc）5 项 finding——全部随 amend 修复（测试重复 import 破坏 Node ESM、zhPool JSDoc 过时、两处内联合并提取为 sourceGroups、types.ts sourceRef JSDoc 错位、limit 语义）。**落库插曲**：主 checkout 的 asset-sourcer.test.mjs 存在来源不明的未提交删除（#183 CDP_VIDEO_SOURCES 测试块，mtime 10:49 与并行 session 1f195cb 提交窗口吻合）——已备份补丁至 /tmp/asset-sourcer-test-deletion-backup.patch 后恢复文件并落库，未采纳该删除。**AFK + grill 队列全部清空**。**⑩ #156 ✅ 交付并关闭**（commits `053b665`+`bd8fa2c`+`bd…fix` 已推送；GitHub #156 CLOSED；按批准设计，HTML 等价 N/A 已批准）：垫层合成——① b-roll orchestrator `assignWinner` 三态落位（`media`/`backdrop`/null 幂等，JSDoc 化）：已有 media 的 b-roll scene 赢家写入 `media.backdrop`（GPU 花了不再扔），report 记 `landedOn`（幂等重跑经 prevEntry 保留旧值）；`planScenes` 保持 asset-then-broll+media 跳过（T8 fixture 契约：asset 赢即不烧 GPU——与 issue 隐含 scene 6 场景的偏差已批准）；② `MediaBackground` 渲染 backdrop 于 primary 之下（全幅、静音、opacity 0.5「不抢戏」），primary 的 overlay 仍管文字对比度；③ `render-remotion` 拷贝 backdrop 文件（不 upscale）+ 缺文件丢层警告；④ `validateMedia` backdrop 缺文件同档 WARN；⑤ `MediaField.backdrop` 类型。**HTML 等价 N/A**（mediaLayer 已退役零消费者，偏差已批准）。**渲染冒烟揪出两个层序事实**：Remotion `CanvasImage` 合成永远在 `<Video>` 之下（与 DOM 顺序/zIndex 无关）→ 有 backdrop 时 primary 图改走常规 `<Img>`（无 backdrop 零回归）；品牌 matte 是裸背景 → backdrop 画在 matte 之上、primary 之下。视觉证据：不透明全幅 chart 下 backdrop 正确隐藏；窄幅 chart 两侧透出 0.5 透明度运动层（「不抢戏」验收达标）；真实 orchestrator 冒烟：qwen4 scene 8 缓存赢家路径 primary 不动、backdrop 落位（真实视频文件）。验证：TDD 5 新项 + 1 契约改写 + 受影响 156/156；全量 2540 绿 + 50 已知环境类失败。**#179/#196 状态不变**（ready-for-human / 待裁决）。**⑪ #196 ✅ 交付并关闭**（commits `2e2815a`+`6494565` 在分支待落 main；GitHub #196 CLOSED；用户批准方案 B）：LFS 治理——`.gitattributes` 移除全部 `filter=lfs` 规则（替换为禁再引入说明），`git add --renormalize` 把 15 个真 LFS 指针文件转成 raw 内容（合计 7.0MB：dh-fixtures/sensetime 图/unitree-superman 等）；旧的 20 个 raw 二进制从此与规则状态一致。**验收**：新 worktree checkout **0 脏文件**、bgm/logos/unitree/dh-fixtures 全部真实二进制（3.3MB/2.9MB/11.8MB 实测）；`git lfs ls-files` 归零；pre-commit lfs-pointers 检查适配。media-asset-management.md LFS 段落更新为「Binaries in regular Git, NOT LFS + 禁再引入说明」。**用户指示（2026-09-05）**：数字人（#194）与 voice/TTS（#179/#35）的测试由独立 session 跟进，tracker 标记暂缓、优先级最低。**下一 session 候选**：① #97 WeChat RSS、#75 视频源标注（ready-for-human）；② #155 aiImage T2I（backdrop 前奏，ready-for-human）；③ #194/#179/#35 已标记暂缓（等独立 session）；④ agent 可自主票已清空。Previous: 2026-09-04（第十五 session）— **#182 ✅ 交付并关闭**（commit `cfbb2eb` 已推送；GitHub #182 CLOSED）：iesdouyin CDP 下载适配器进统一下载层——`downloadDouyinCdp`（`cdpNewTab(iesdouyin share URL)` → `waitForPageLoad` → **页内轮询** `<video>.currentSrc`——share 页 302 到 douyin.com 后 xgplayer 懒水合 ~6s 才有 src，实测发现；extractFromTab awaitPromise 单次往返）→ `downloadDirectHttp` + `Referer: https://www.douyin.com/` → finally 关 tab，免登录；`selectStrategy` 2.5 路由 douyin/iesdouyin → `douyin-cdp`；cobalt preflight 收敛到 cobalt 路由（douyin 不再白探 localhost:9000——Spec review 记录的 accepted scope note）；`extractFromTab` 数组契约：脚本返回 `[src]`，空/白数组 → `no-current-src` retryable（真实冒烟抓到的回归：裸字符串 return 被 JSON.parse 吞成 `[]`）。验证：TDD 13 新项 red→green（video-downloaders 67/67）；真实数据冒烟样本 `7680095489249536842` → **470,028 B mp4 / 5.92s（ffprobe 验证）/ 6.5s 端到端**；全量 2868 绿——残余失败复现为并行负载时序抖动（rate-limiter/asset-sourcer-integration/pool 隔离重跑 82/82）+ worktree 环境类 render 测试，与 #193 轮同模式；code-review 两轴（006427a...cfbb2eb）无阻塞 finding（Standards 仅既有 isXxxUrl 模式内 judgement call）。VD-03/VD-11 cobalt 兜底测试样本 douyin→vimeo（douyin 已有专属 adapter）。**下一 session 候选**：① W2 剩余 AFK 票基本清空——#97/#75/#186 均 ready-for-human 或需讨论，#191/#192/#185 动手前需 grill/design；T3 AFK 候选：#92 SearXNG、#156 垫层合成、#98 P5 ASR（大）；② 指南 commit `6a1e127` 合并（仍等用户 WIP 落库）。Previous: 2026-09-04（第十四 session）— **#193 ✅ 交付并关闭**（代码 commit `87f1a8d` 已推送；GitHub #193 CLOSED；用户裁决 option D：lint WARN + 指南，拒绝 A/E）：`checkMediaStrategyContract`（scene-rules.mjs，已由 runAllSceneDataChecks 接进 verify-video --pre preflight）对省略 `mediaStrategy` 的 scene 聚合发一条 WARN（列全部 scene id + 三个显式值 asset / asset-then-broll / b-roll），取代静默 `"asset"` 缺省——didi-robotaxi-r2 scene 3 图片复用根因。有意行为变更：取代旧 b-roll spec 行 #2（静默）/#6（implicit pass），存量包 preflight 将出现该 WARN 直至 scene-data 显式设值；placement 偏差（preflight 而非 issue scope 的 orchestrator.planScenes）实施前已报备用户。验证：TDD 2 新项 red→green + 全量 2855 绿（残余失败为 worktree 环境类：真实 Chromium render + gitignored 媒体，主 checkout 同五文件 baseline 仅 1 已知抖动）；真实冒烟 qwen4/zhipu preflight WARN 按预期、exit 0 不变、0 FAIL。**指南 commit `6a1e127` 待合并**：在分支 `session/20260904-media-strategy-warn-193-7266f0`，被 `docs/video-script-writing-guide.md` 的非本 session 未提交 WIP 挡住 ff 合并——WIP 落库后 `git merge --ff-only session/20260904-media-strategy-warn-193-7266f0` 即可。code-review 两轴（387870d...6a1e127）无阻塞 finding；ponytail-lite 试点记录已追加 #193 行。**下一 session 候选**：① W2 其余：#97/#75/#182/#186（#191/#192/#185 动手前需 grill/design；#182 blocker #181 ✅ 已解除）；② 指南 commit 合并（等用户 WIP 落库）。Previous: 2026-09-04（第十三 session）— **#181 ✅ 交付并关闭 + #187 ✅ 关闭**（commit `0185184`；GitHub #181/#187 CLOSED）：① #181 全平台下载方法实测（固定公开样本，/tmp 验证不落库）：YouTube `aircAruvnKk` → mp4 1119.9s/9.42MB ✅、B站 `BV1DPbc68EjK` → mp4 163.1s/8.98MB ✅（yt-dlp 2026.07.04 + Firefox cookies）；小红书 rednote-mcp `--stdio` + `search_notes` ✅（2026-08-21 cookies 仍有效，3 条完整笔记返回；bin 必须带 `--stdio`、参数为复数 `keywords`）；**Cobalt v11.7.1 降级**——YouTube 返回 tunnel URL 但拉流 0 字节（HTTP 200 空 body）、TikTok `fetch.fail`、抖音 `link.invalid`（不在 services），维持「不作为核心依赖」；抖音/TikTok 沿用 2026-09-03/2026-08-24 已录证据未重跑（需 CDP 浏览器会话）；`asset-source-quick-reference.md` 状态列 + 验证日志块已更新（docs-only R1：lint:docs + diff check 通过，WARN 为既有无关项）。② #187 关闭：全仓 grep 证实无全文提取消费方（`fetch-page.mjs` 唯一引用方是自身测试），`fetch-page.test.mjs` 17/17 绿——#66 交付的四层基础设施健康，但接线对象不存在，无 consumer 的净新功能不做投机实现（出现真实消费者如 RAG claim verification 时 reopen）。**下一 session 候选**：① W2 其余：#97/#75/#182/#186/#193（#191/#192/#185 动手前需 grill/design；#182 的 blocker #181 ✅ 已解除）。Previous: 2026-09-04（第十二 session）— **#180 ✅ 交付并关闭**（commits `9f46145`+`fe70670`；GitHub #180 CLOSED）：Bilibili 视频搜索中文关键词路由 + yt-dlp 解析根因修复——① zh-CN 视频源（source-registry `locale: "zh-CN"`，flattenYtdlpVideoSource 透传）路由到中文公司池：`buildZhVideoKeywords`（extractKeywords 3-tier × COMPANY_NAME_ZH 映射，池空回退原关键词组）+ `pickVideoKeywordGroups`（纯函数路由）；YouTube 保持英文组不变；② 根因修复：yt-dlp 2026.07.04 对 --print 模板 `\t` 字面输出 → 旧解析器 split 失败，双平台所有候选 id=整行、title 空、url 带 `\tNA\tNA` 垃圾（didi 缓存实证）——提取纯函数 `parseYtdlpSearchOutput`（真实 TAB split + NA 占位 + 旧格式行跳过 + 标题含 TAB rejoin），bilibili 改非 flat 搜索 `--playlist-items 1-6`（flat 条目无标题，评分永远 3 < preFilter 20）。验证：TDD 15 新项全绿（zh 池 4 + 路由 3 + 解析 8）+ 受影响 450 + 全量 2332 绿（唯一失败 scene-gate-render 已知 Chromium 抖动）；真实数据：didi-robotaxi-r2 管线日志证 bilibili 用中文池（滴滴/小马智行/百度/特斯拉各 1 实时候选，Waymo/广汽埃安 0）、YouTube 英文组行为不变；live：bilibili 滴滴 → 有标题候选 score 31 过 preFilter → 8s 片段下载 695KB 成功；YouTube 标题恢复（此前全空）。**YouTube 有用性评估**（issue 要求）：flat 标题恢复后公司名单词关键词可过阈值（waymo→28+3=31），多词英文短语关键词 title 部分匹配仅 14-17 < 20——YouTube 对中文话题可用但依赖关键词形态，深水区属 #185。code-review 两轴（区间 a314369...fe70670）：Standards 无硬违规（标题含 TAB 回归已修）、Spec 无缺失（claim 归属变化如实报告：公司名搜索天然非 scene 专属，原 claim 绑定候选即被 VLM 拒绝的垃圾，实际无损）。**下一 session 候选**：① W2 其余：#97/#75/#181/#182/#183/#184/#186/#193（#191/#192/#185 动手前需 grill/design；#182 blocked by #181）。Previous: 2026-09-04（第十一 session）— **#112 ✅ 交付并关闭**（commit `d74ad76` 已推送；GitHub #112 CLOSED）：Tier 3 可插拔图片搜索引擎池——`IMAGE_SEARCH_ENGINES` 注册表（progressive-search.mjs）：brave_image → searxng_image → google_images（CDP `tbm=isch` + imgurl 全分辨率）→ bing_images（CDP `a.iusc` murl 全分辨率）→ duckduckgo_images（纯 fetch vqd+i.js，用户确认偏离 issue 的 CDP 方案；本机网络封锁时按设计降级空数组不缓存）→ tavily_images（include_images 兜底末位）；asset-sourcer 消费引擎池，引擎间 Promise.allSettled 并行、同引擎内关键词串行；新增引擎只改注册表不动 asset-sourcer。TDD 5 slices 65/65 + 全量 2318 绿（唯一失败 scene-gate-render 为 pre-existing Chromium 抖动，与本票零交集）；真实冒烟 kimi-ipo `--max-per-source 1`：Tier 3 实际触发，Bing 243/Google 106/Tavily 35 候选，6 张新引擎资产下载过 VLM 分析。code-review 两轴（Spec 无缺失/Standards 无硬违规）修复：CDP fallback 脚本去重（GENERIC_IMAGE_FALLBACK_SCRIPT）、normalizeCdpImageCandidates 收紧 http(s) 契约（补 protocol-relative/ftp 拒绝测试）、DDG #91 注释修正、middle-man 包装消除。**下一 session 候选**：① W2 其余：#97/#75/#180–#186/#193（#191/#192/#185 动手前需 grill/design）。Previous: 2026-09-04（第十 session）— **#190 ✅ 交付并关闭**（commits `0233f2f`+`ee3ec03` 已推送；GitHub #190 CLOSED state_reason=completed）：MRL-2 preflight 接入 template-contract 校验——`checkTemplateContract()` 进 `runAllSceneDataChecks`（verify-video --pre → main.mjs，TTS 前），对每 scene 调渲染层同一 `assertKnownTextFields()`；布局解析提为共享 seam `resolveRenderLayout()`（text-slots.mjs，与 ShortVideo.tsx 单一事实来源，消除两侧漂移）；未知/未测量字段与缺 rendered 字段 FAIL（携带渲染期同一错误消息），非 narrative 死 layout 值 WARN（渲染忽略该字段——issue 类别 4 按渲染证据降级，fix 文案指向渲染值避免与 checkLayoutField 的「缺 layout 即 FAIL」矛盾）。TDD 11 新项（kimi-ipo 四类违规重放 red→green）+ 受影响 249 全绿；全量 2843/2845（唯一失败 #189 pool timing，孤立重跑 8/8，负载抖动非本路径）；tsc 干净；真实数据冒烟 qwen4/zhipu/kimi-ipo exit 0（kimi-ipo 事件后已合规），bytedance-distillation exit 1 为 legacy 既有（decision 47），新检查在其上额外抓出 speaker/leftAction 未注册字段。**附带修正**：overlimit fixture CTA texts（`line1` 未注册、缺 brand/tagline）与 scene-rules validScenes 三场景渲染必炸数据——旧 preflight 从不拦截的同类违规。**review**：Standards+Spec 两轴并行 sub-agent（区间 `b6ecee1...ee3ec03`），1 hard finding（WARN fix 文案矛盾）已随 `ee3ec03` 修复。**下一 session 候选**：① W2 其余：#97/#112/#75/#180–#186/#193（#191/#192/#185 动手前需 grill/design）。Previous: 2026-09-04（第九 session）— **#189 ✅ 交付并关闭**（commits `ce758a6`+`24b9bf5`+`d12e32e` 已推送 `0ce990b`；GitHub #189 CLOSED state_reason=completed）：Node 侧 VLM worker pool（`VLM_CONCURRENCY` 默认 2，per-worker pending Map，Python 零改动）+ VLM 结果缓存（`lib/vlm-cache.mjs`，key=promptVersion+model+fileFingerprint+window/claim，大文件 >16MB 用 size+mtime 跳全量 hash）+ asset-sourcer Phase 3a 并发接线；TDD 19 新项（pool 8+cache 7+wiring 4）+ 既有 visual-analyzer.test 38 项锁 pool=1 + 集成 51 项补 `getVlmConcurrency` mock；全量 2832/2833（唯一失败 scene-gate-render 已知 Chromium 抖动）；真实数据冒烟 4 图：串行 396.5s → 并发 130.7s（3.0x）→ 缓存重跑 1.4s。Scope 收窄：候选 3（fast-path 语义规则未定）/4（escalation 阈值需 benchmark）/5（mlx-vlm 无 true batch）未做。**git 事故记录**：review 修复阶段 `git commit --amend` 误 amend 并行 session 的 fix(provenance) commit（d9ce6b1），已 `reset --soft` 恢复——我的修复独立 commit `24b9bf5`，并行 session fix provenance 工作保留工作区未 staged，push 前操作无 remote 损害。**下一 session 候选**：① **#190 MRL-2 preflight 契约校验**（Tier 1，防整轮 TTS+render 浪费）；② W2 其余：#97/#112/#75/#180–#186/#193。Previous: 2026-09-04（第八 session）— **① #179/#189–#192 五票 triage 补录完成**（Tier 1 增 #189/#190，Tier 2 增 #179/#191/#192；Matrix 增 visual-analyzer/vlm_analyzer 行；W2 增补 didi/kimi-ipo 批次含此前漏录的 #180–#186/#193；GitHub 标签已按成熟度补齐：#189/#190 `ready-for-agent`、#191/#192 `needs-triage`）。**② #65 ✅ 交付并关闭**（commits `5c6fd13`+`de92644`）：REST pool（Brave>Tavily>Jina）+ Step 3 接线 + search-pool-server stdio MCP（**#109 随之 auto-close**）+ isMainModule guard 修复；TDD 27 项 + 受影响 187 全绿；live 冒烟全量 trend run 四源 Layer 3 真实命中，Brave TUN `fetch failed` → Tavily 自动接管（环境约束如实记录于 pool 注释与 tracker）。**下一 session 候选**：① **#189 VLM 提速**（W2 Tier 1，didi/kimi-ipo 生产痛点直接来源，15min→5-7min）；② **#190 MRL-2 preflight 契约校验**（Tier 1，防整轮 TTS+render 浪费）；③ W2 其余：#97/#112/#75/#180–#186/#193。Previous: 2026-09-04（第七 session 续）— **#188 ✅ 交付并关闭**（commit `d4b1e38` 已推送）：verify-lfs fixture 补 unset `process`/`required`（TDD red→green，4/4；全量 2787/2794，唯一失败为 scene-gate-render Chromium 环境抖动）。**同日并行 session 新建 4 个 issue 尚未入 tracker（待 triage）**：#179（TTS 引擎测试批次，ready-for-human）、#189（asset-sourcer VLM 串行 15min）、#190（MRL-2 preflight 缺 template-contract 校验）、#191（mediaOptOut preflight warning）、#192（asset rejection feedback loop）——下一 session triage 时按 Protocol step 4 补录 Tier/Matrix。**下一 session 候选**：① **新 issue triage 补录**（#179/#189–#192）；② **#65 General Search Pool**（#90 ✅ 已解除依赖；engines 凭据已就位于仓库根 `.env.local`）。Previous: 2026-09-03（第七 session）— **#90 ✅ 交付并关闭**（commit `4d236b3` 已推送 origin main）：`lib/bigsong-api.mjs` 直连迁移（searchX/searchXhs，60s timeout）+ registry/search-sources 契约接线（Step 2.5 apiFallback、共享 `parseTweetList()` 提取、auto-gen 跳过 apiFallback 源）+ TDD 8 项 + 受影响 222/222。**核实闭环**：上轮停报要求的事实双源核实已由真实数据冒烟补齐——`searchX("DeepSeek V4 release date")` 1 轮 live：`success: true`、正确带引用答案、端点在线（issue 数据中的 0–20% flake 未复现）；「代码库无引用」为预期——客户端本票新建。mcp-client.mjs 留作 rednote-mcp。**下一 session 候选**：① **#188 verify-lfs fixture 修复**（bug；全量套件当前即失败项）。Previous: 2026-09-03（第六 session）— **文本溢出 epic 收官批次全量推送 origin main（efc62e4..d23b7f7，含 T11 `c850113`/`845f5ff`、#127 `fc40dae`、#171 `2c5b3d9`、#91 `653e508`、#128 `2c34638`、widgets review `b753caa`/`d23b7f7`、git-workflow 并行隔离规则 `5b94605` 等）**。**#178 新建**：docs-lint gate 对 tracker Last-inventory 轮换行的 WARN 噪音治理——每轮 `docs(tracker)` 例行轮换必命中反引号路径模式弹非阻塞 WARN，提议「替换（存在新 Last inventory 行）豁免、净删除仍 WARN」，实施前过 proposal-review；认定 WARN 作确认检查点值得保留亦可 close 不做。**handoff-text-overflow-hardening-2026-08-31 归档**至 `docs/archive/handoffs/`（#141 收官后按 DOCS-INDEX 惯例关闭 handoff；其头部已标注「epic 全部收官、转历史参考」，archive README 登记行由并行 session re-land）。**同日事故记录**：多 session 同工作树并行，本 session 与并行 session 的 rebase/amend 相互覆盖（本 session tracker commit 两次丢失、archive commit 一次混入他人 WIP，均无损重建）；教训已由并行 session 落入 `5b94605` git-workflow 并行隔离规则。**下一 session 候选**：① ~~**#178 实施**~~ ✅ closed（commit `4be7963` 已随 `ebc6fb3..29fdfc5` 推送）；② ~~**#177 prettier 漂移**~~ ✅ 部分交付（commit `bf6bea2` 已推送；pin 落地但 lint-clean 未达成，余 1190 条待决策）；③ ~~W1/W2 大特性 #66/#64/#90~~（#66 ✅ 关闭；**#64 ✅ 代码完整** commit `acf9dd8` 已推送，真实数据待复测；#90 需人工决策跳过）Previous: 2026-09-03（第五 session）— **#91 ✅ 关闭 + #128 关闭（见下）**。#91 DDG（commit `653e508`）:html 非 JS 端点 + uddg 解包 + rate-limiter + attributions;真实 CDP smoke 10 条 0 wrapped;#112 hard blocker 解除。#128 SVE item1+4(commit `2c34638`):data URI 排除 + CDP_MEDIA_CAPABILITIES。Earlier（第四 session）: **#127 ✅ 关闭**。按推荐路线先抽 `deep_analyze()` 再 close：commit `fc40dae`（已推送）——`run_vlm_inference()` 共用 seam + `deep_analyze()`（issue 正文要求的函数名），`handle_analyze_semantics` ~90 行 2B/deep 重复预处理块消除；TDD `test_cascade_inference.py` 8 项 red→green，python 36/36 + Node `visual-analyzer.test.mjs` 38/38 + 管线 venv 真实 smoke 两条（unitree 2B 快路径识别正确；纯色图逼出升级信号 → RAM 门控优雅回退）。Router 第 4 信号从未实现，superseded。**下一 session 候选**：① **#152 T11**（Tier 1 最后一票，T8/T9 已于第三 session 关闭，已解锁；含 #153 遗留 slotCharBudget 重构）；② **#177 prettier 漂移**（需用户决策：锁版本 vs 全量 reformat）；④ W1/W2 大特性 #66/#64/#90（均 ready-for-human）。**#171 ✅ 同 session 关闭**（commit `2c5b3d9`：6 项 findings 中 3 项已随 eeab5ec 解决，补齐 git 模式显式测试 + messageOf() helper + DiffLine typedef，35/35）。Previous: 2026-09-03（第三 session）— **#149 T8 ✅ 交付并关闭、#150 T9 ✅ 关闭，全批次已推送 origin main（c5d38b4..2a0b86a）**。T8 交付 commit `2a0b86a`：highlight 结构化 `{field,text}`；`assertKnownTextFields` 结构/字段/子串 fail-closed 校验；`SplitHighlight` 只标注子串（NarrativeScene result/action、HookScene hookText，前后片段原样渲染）；24 处存量迁移并逐条过子串校验（qwen4 7 / doubao 9 / light-society 1（改写 quote「4M beliefs」）/ zhipu 6 / _gate-smoke 1）；gate 修复——settled annotation assert 改为「svg 存在即启动/接替轮询」（T10 `key={fontSize}` 重挂载在 group 缩字连步时留下未绘制 SVG，旧轮询空读 3 帧误判 annotation-missing）；fixture 补 F1 整行 fragment 重放事故、group-shrink 期望按可换行几何重校（16/18/50 → 113 ≤ 116）。验证：392 tests 全绿（scene-gate-render 24、text-gate/契约/模板 115、scene-rules/narrative/safezone 211、parity/timeline/transitions/official-fit 42）+ `tsc --noEmit` 干净。**下一 session 候选**：① **#152 T11**（端到端重渲染 + 文档归档，Tier 1 最后一票，关 #141 前必做；含 #153 遗留 slotCharBudget 重构）；② **#127 决策**（close vs 先抽 deep_analyze()，见 Tier 2 行）；③ **#177 prettier 漂移**（先决策锁版本还是全量 reformat）；④ W1/W2 大特性 #66/#64/#90。Previous: 2026-09-03（第二 session）— **#151/#164/#153/#129 CLOSED，#165 → Dormant，#177 新建**。#151 T10 核验关闭（da2cacf + 62/62 复验）；#164 eslint ignores experiments/**（lint 45min 不收敛 → 14s）；#153 部分交付后按决策 47 处置关闭（决策 14/71 预算 WARN 代码落地、15 包盘点交付、guard-cli 7/7 恢复绿；存量包 layout/visualType 迁移不实施，11 包红为预期）；#129 归档误报修复；#165 复核触发未满足转 dormant；#177 新建（prettier 3.9.6 环境漂移 → 1169 既有 findings）。**下一 session 候选**：① **T8/T9/T11（#149/#150/#152）**——并行 session 2026-09-03 已在做（工作树 WIP：text-slots badge、substring-highlight、Hook/Narrative/TextGate），等其提交后接手 T11 收尾（含 slotCharBudget 遗留重构，见 Tier 1 #153 行）；② **#127 决策**（close vs 先抽 deep_analyze()，见 Tier 2 行）；③ **#177 prettier 漂移**（先决策锁版本还是全量 reformat）；④ W1/W2 大特性 #66/#64/#90。Previous: 2026-09-02 — **全量 re-inventory（脚本集合比对）**：GitHub open 48 与 tracker open 48 **集合一致**（上一版缺 9 行：#141 spec parent、#149–#153 文本溢出 ticket 批次、#165/#171 lint 清理、#175 T12——本次全部补录进 Tier 1/2/3；#159 GitHub 已于 2026-09-02 close，与 tracker 同步）。**#175 T12 ✅ 已交付并关闭**（commit `44fa8da` 已推送；收尾 `a1cf1c8` 探针 + bigNumber 150 放宽决策 72、`bac686e` 音频验证器修复、`8786eb3` 本票记录，三提交均 `git push origin main`）。文本溢出 epic 各票补录见 Tier 1 专块。**#176 ✅ 已交付并关闭（2026-09-03 session）**：根因定位为 Remotion raw 音频轨自带 86–101ms 可变前导延迟（mp3 解码 delay + AAC priming 叠加，每次渲染不同；post-process 链不追加不修正）。修复：`lib/audio/sync.mjs` 新增 `realignAudioToTimeline()`（FFT 测漂移中位数 → 常量头部 `atrim`/`adelay` → 漂移非恒定或 <5ms 拒动），接入 `render-remotion.mjs` 后处理末端。验证：audio-sync 26/26 + 音频域 71 tests 全绿；`_gate-smoke` 实渲染 9 scene audioSync 0 超差 PASS，成片前导静音消除（首段静音 0.435s = 语音内自然停顿）。Previous: 2026-08-30 — **#113 VLM 图片预处理 ✅ CLOSED**（Triage Protocol step 3：实现早已在 `main`，本 session 只做验证与关闭——代码核对 + `test_resize_image.py` 5/5 + `unitree-building.jpg` 3468×4624 真实数据冒烟确认无幻觉）。**#127 核实结论已写入 Tier 2 行**（级联端到端跑通，残留两项差距待决策）。Previous: 2026-08-30 — **#89 P0 rate limiter ✅ done**（新模块 `rate-limiter.mjs` + `cdpNewTab` 集成，47 tests + 真实数据冒烟，spec/tickets/review 已归档到 `docs/archive/`）。GitHub #89 CLOSED。Previous: 2026-08-28 全量核对完成。**#140 已创建**（承接 #89 closed 后失去 tracking 载体的 P1/P2/P4–P7 切片；同时更正「P5 = `proxy-manager.mjs`」这一旧误标——#89 原文中 P5 是 selector auto-healing，proxy-manager 属 P6）。**2026-08-30 re-inventory（脚本双向集合比对，非人工点数）**：GitHub open 33 与 tracker open 33 **集合完全一致**——既无「tracker 列 open 但 GitHub 已 closed」，也无「GitHub open 但 tracker 未收录」；Closed 表 49 行全部确为 GitHub closed；open/closed 表无重叠、Tier 表无重号。**tracked = 33 open + 49 closed = 82**（= Closed 表行数 49 + tracker open 行数 33，两边已统一）。GitHub `closed` 计数 99 比 49 多 50，构成为：**40 个从未在 tracker 出现过**（tracker 建立前的历史 issue）＋ **`#15`**（仅作为 #111 的 Blocked-by 依赖被引用）＋ **`#131`–`#139`**（#130 的子 ticket，随 #130 一并关闭，未单独立行）——后 10 个不算 tracked，但**不再是「从未提及」**（旧表述不准确，已更正）。**下一 session W1 候选：#127**（串行约束已随 #113 关闭解除，且已有端到端验证证据；待决策：直接 close 还是先抽 `deep_analyze()`）或 **#66**（同为 T2，改 `cdp-client.mjs` 等，🔴 高冲突）。#88 Part 2 completed (commit c177213): auto-gen googleSiteFallback for 13 sources, W0 fully done. Previous: #111 CLOSED, #129 created, #63/#115/#116/#114 CLOSED, #128 created, #120-#126 all CLOSED, #127 created, #119 fully closed, #75 promoted to Tier 2, #117 created, #113 VLM image preprocessing, #63 split into #63+#114, #65 renamed, #110 closed, #112 added, #109 merged into #65, #67/#78/#83/#81/#22/#62/#70/#51 Closed.


---

# 下沉追加（2026-09-06 第二十一 session 清理）

Last inventory: 2026-09-06（第二十 session，agent marathon）— 用户授权马拉松模式（多 issue 连做、子代理并行、一般问题自决）。**#201 ✅ 交付并关闭**（commit `2c88ab4`）：渲染回归根因 = bd8fa2c 的 `zIndex:1` 把 primary media 抬到 dim overlay 之上，删除后 qwen4-preview 帧验证恢复 71/71；**#207 ✅ 交付并关闭**（commit `c071735`）：MuAPI 异步编排模式调研笔记落 docs/research/muapi-async-task-patterns.md（取舍表 4 可用/5 不抄，remote-task.mjs 落地建议待独立 issue）。**#204 进行中**（agent 实现 Telegram 源）。Commits `2c88ab4`/`c071735` 本地 main 未推送。字幕 verify 既有 2 error（旁白停顿）再次确认非回归。


Last inventory: 2026-09-06（第十九 session 收官·re-triage）— 账号安全风险评估落地：**xhs 下载 CDP 通道设为首选（低风险），XHS-Downloader 降为显式 fallback**（签名 API 调用检测面大，与 Auto-Redbook 红线对照）；护栏待设计进 #209。Re-triage 变更：#201 加 ready-for-agent（W2 高优先，回归阻塞质量验证）、#204/#207 ready-for-agent、#208/#210 → dormant、#140↔#209 互链（P1/P2/P6 待 #209 吸收）、#35 注记基础设施已交付、#60/#61 维持 triage 待用户。**新增入账**：#201/#204/#207/#209 补登 Tier 2 表（此前平行 session/本 session 创建未登记）。Agent 可执行前沿（按 wave）：#201（回归修复）→ #204/#207/#209 → #203。

Last inventory: 2026-09-06（第十九 session 收官）— **#75 ✅ 交付并关闭**（GitHub #75 CLOSED；本 session commits `8b7e0ae`→`203766c` 全部已推送）：国内平台视频下载三平台集成完成（douyin/weibo/xhs 均实测下载成功）+ baidu_news 标注 + 审计测试缺口 3 项。grill 裁决落地：MCP Bridge 实测（SEARCH_TOOLS 多路由按工具名区分，后端无 xhs 通道）、XHS-Downloader 引入为主后端、`XHS_COOKIE` 已持久化 `.env.local`（gitignored）。**机制发现**：xsec_token 短时效绑定上下文。**session 遗留**：qwen4-preview 帧验证 6 FAIL → #201；三平台 capabilities.videos 标注待搜索侧验证（新票可开）；tiktok_creator 待配额授权。全量 vitest 2686 passed / 4 已知 env-flake。

Last inventory: 2026-09-06（第十九 session 终·xhs 打通）— **xhs 端到端下载实测通过**（commit `7bf88eb` 已推送）：按「重没关系主要看好用」裁决引入 XHS-Downloader 为主后端（`~/tools/XHS-Downloader` + venv，`XHS_COOKIE` env 完整登录 cookie），VDL 路由 xhs → downloadXhsDownloader（主）→ downloadXhsCdp（降级，重构为 feed 预热+同标签导航模式）；live 证据：两条真实视频笔记 28.8MB/31MB mp4 下载成功。**机制发现**：xsec_token 短时效绑定上下文，下载必须用采集时同步拿的新鲜 token。MCP Bridge 实测：SEARCH_TOOLS 多路由按工具名区分，但后端无小红书数据通道（rednote 路由 120s 挂起、通用路由零 xhs 链接）。#75 剩余：XHS_COOKIE 持久化、xhs 搜索侧 token 稳定性验证（→ capabilities.videos 标注）、tiktok_creator 配额实测。

Last inventory: 2026-09-06（第十九 session 终·#75 专项）— **#75 主体交付、保持 open**（commits `8b7e0ae`/`d22c100`/`34e43ce` 已推送）：Batch1 baidu_news media entry（#77 疑点1 修复 + same-DOM 守卫 + 零 fallback 9 源快照锁 + `pexels_video` typo 修正）+ collectFromSource 全链集成测试 14 项；Batch2 weibo 路由进 ytdlp adapter（yt-dlp 原生 weibo extractor，`--playlist-items 1` mix_media 主视频，WEIBO_COOKIE→Netscape 文件，cookiejar 前导点坑已修）；Batch3 xhs RedNote-MCP adapter（get_note_content→CDN 直链→Referer 下载，fail-closed）。**剩余全部阻塞在浏览器登录态**：weibo 完整下载证明（需导出 cookie 或钥匙串授权 chrome）、xhs 登录刷新（rednote-mcp login 扫码）；三平台 capabilities.videos 标注按 #77 原则待视频搜索路径验证后同步落地；tiktok_creator 文档 SPA 不可取，留配额实测。全量 2686 passed / 4 已知 env-flake。

Last inventory: 2026-09-06（第十九 session 终，grill 后执行）— 用户裁决 6 项：push ✅（d7352b2..3a92094）、#179 ✅ 关闭、#35 基础设施 ✅ 交付（`11c7a6f`：REF_AUDIO_MAP + resolveRefForScene fallback + buildF5Manifest 按需 stamp + python resolve_item_ref/memoized probe；等用户录 ref-styles/{hook,narrative,data,cta} 四段 10-15s 音频后做 A/B）、#194 维持暂缓、qwen4-preview 帧验证回归开票 **#201**（c90e249 后仍复现，确定性布局回归，raw 即违规）、#75 下个 session 专门 grill/design。

Last inventory: 2026-09-06（第十九 session 续，agent marathon）— **#200 ✅ 交付并关闭**（GitHub #200 CLOSED；commit `6edd5bc`，本地 main 未推送）：source health log 三项建议全交付——collectFromSource per-layer trajectory（含 #66 同 URL 跳过显式标注）、discovery.sourceHealth 轨迹+零结果清单、lib/source-health.mjs 连续零结果 streak 落盘与 ≥3 run 复核提示。至此三个审计 follow-up（#198/#199/#200）全部关闭。测试 11 项新增；全量 2665 passed / 1 env-flake（scene-gate-render Chromium 停滞）。

Last inventory: 2026-09-06（第十九 session 续，agent marathon）— **#199 ✅ 交付并关闭**（GitHub #199 CLOSED；commit `f437c5b`，本地 main 未推送）：SSOT follow-up 8 项全交付（高影响 2：NO_MEDIA_TYPES 单例 import、discovery schema gate 进 writeResearchArtifact；低影响 6：MediaCandidate typedef、trending-topics 读侧 warn、asset-report/media-patch schemaVersion envelope+normalizeMediaPatch 兼容层、cap 双读规则注释、KNOWN_COMPANIES/MediaField/tts-types 注释）。顺带修复 4a4439f 的 vi.mock 缺 DEFAULT_VLM_MODEL_ID 导致的 45 测试并行失败（asset-sourcer-visual-integration + asset-sourcer-cache 两 mock 工厂补常量）。全量 3101 passed / 1 env-flake（scene-gate-render Chromium 停滞，见 test-env-baseline）。
