# 01 — DOCS-INDEX.md Layer Placement Rules + Semantic Labels

**What to build:** Add "What does NOT go here" column to all 4 layer tables in `docs/DOCS-INDEX.md` Canonical Structure. Add semantic labels (L1: Execution reference / L2: Deep research). Add a `### Layer Placement Rules` sub-section with 4 rules that tell an agent how to decide which layer a new/modified document belongs to. Add a pointer from DOCS-INDEX.md to AGENTS.md's `writing-for-agents` mandatory-load rule.

**Blocked by:** None — can start immediately.

**Status:** ready-for-agent

- [ ] L0-L3 tables each have a "What does NOT go here" column
- [ ] L1 Purpose cell says `**L1: Execution reference**`
- [ ] L2 Purpose cell says `**L2: Deep research**`
- [ ] `### Layer Placement Rules` sub-section exists under Canonical Structure
- [ ] 4 rules present: (1) L1 only execution, (2) L2 only research, (3) new doc: ask which, (4) modifying L1: check for L2 intrusion
- [ ] Rules end with pointer to AGENTS.md `writing-for-agents` mandatory-load rule
- [ ] No existing table rows or pointers are modified (pure addition)
