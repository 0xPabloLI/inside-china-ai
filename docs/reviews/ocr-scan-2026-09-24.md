# OCR 审查报告 — 2026-09-24

**工具**：open-code-review v1.12.9
**模型**：`ornith:1.5-9b`（Q4_K_M，5.4GB，从 HuggingFace 导入 Ollama）
**日期**：2026-09-24
**Session**：`af107963-37cb-4ef0-8608-09b3d5b79407`

## 审查范围

当前工作区改动（16 file(s) changed, 3 selected for review）：

| 文件 | 改动 | 状态 |
|------|------|------|
| `scripts/short-video/lib/asset-sourcer.mjs` | +1 -1 | reviewed, 0 findings |
| `scripts/short-video/lib/vlm_analyzer.py` | +65 -247 | reviewed, 0 findings |
| `skills/open-code-review/open-code-review` | +1 -0 | reviewed, 1 finding |

被过滤的文件（15）：docs/*.md（unsupported_ext）、package-lock.json（default_path）、test 文件（default_path）、prototype.html（too_large）。

## 发现

### 1. `skills/open-code-review/open-code-review:1-1` — broken symlink

**严重度**：bug · low

**描述**：符号链接目标路径不存在。从 `skills/open-code-review/open-code-review` 出发，`../../` 落到 `skills/`，拼接 `skills/open-code-review` 得到 `skills/skills/open-code-review`，该路径不存在。应指向 `../../skills/open-code-review/SKILL.md`（实际文件）。当前是 broken, non-functional symlink，可能是 accidental setup artifact。

## 性能

| 指标 | 值 |
|------|-----|
| 耗时 | 10m3s |
| tokens（input） | ~138,539 |
| tokens（output） | ~12,586 |
| tokens（cache read） | ~118,414 |
| tool calls | 29 |
| 超时配置 | `OCR_LLM_TIMEOUT=1200`（20min） |

## 模型选型备注

> **注**：本报告为选型决策前的运行记录；最终采用方案以 `docs/research/ocr-llm-model-selection-research.md` 为准（Q8_0；Q4_K_M 列为快速备选）。

- **Q8_0（9.8GB）**：17.3 tok/s，25min 未完成审查，**不适合**代码审查（M2 Pro 带宽不够）
- **Q4_K_M（5.4GB）**：25.6 tok/s，10min 完成审查，**本次审查运行采用**
- 详见 `docs/research/ocr-llm-model-selection-research.md`