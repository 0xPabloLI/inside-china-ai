# Ponytail-lite 试点记录（2026-09）

> 状态：**active**（试点期，0/5 任务）。提案全文与裁决依据：`docs/research/ponytail-minimal-code-adoption-proposal.md`（L2）。第三方复核材料：`docs/reviews/ponytail-adoption-review-brief-2026-09-04.md`（v2 含 Q1–Q7 裁决落实）。试点裁决完成后本文件归档至 `docs/archive/reviews/`，并删除 `implementation-workflow.md` §9 中的试点路由步。
> **v2（2026-09-04）按第三方复核重写裁决门槛**：原门槛只要求"改变选择"和"实际触发"，不要求结果更好，5 个任务最多是安全 canary，不能据此转正。v2 增加交付确认、spec parity、返工、误报与实际收益五类字段，并把转正门槛改为需要正向证据；未获正向证据时只能延长或标记未验证。
> **当前进度：0 / 5 任务**（2026-09-03 启动）。本表为空，试点尚无本地证据。

## 范围

后续 5 个实施任务：S1 与 S2 各至少 2 个、R2/R3 至少 2 个；不足 5 个按 4 周窗口截断。**5 个任务只够安全 canary（发现明显失败模式），不足以支撑采纳裁决**（复核裁决 Q6）。

## 逐任务记录

每个实施任务收尾时（`implementation-workflow.md` §9 步骤 3）追加一行。字段口径见下。

| task | 日期 | S/R | 规则是否到达（evidence） | A-lite 选择及放弃的替代 | B-lite finding（独有/重复/误报 → 裁决） | spec parity | 返工 | D（bug？触发？） | 实际收益（定性） |
|------|------|-----|--------------------------|--------------------------|------------------------------------------|-------------|------|------------------|------------------|

### 字段口径

- **规则是否到达（evidence）**：本次任务中规则是否被实际读取或引用，以及证据来源（如"日志出现选择顺序推理"、"无任何相关推理"）。**没有 evidence 就填"无"，不要推测**。此栏用于区分"规则无效"与"规则没被读到"——两种情况在 finding 数上表现相同（复核裁决 Q5）。
- **A-lite 选择及放弃的替代**：最终走了哪一档、最初倾向是什么、是否因规则改变了选择。未改变填"未改变选择"。
- **B-lite finding**：标注 **独有**（smell 基线不会报）/ **重复**（与 Fowler 基线重叠）/ **误报**（报告了不存在行为等价替代的项），以及接受或拒绝的理由。
- **spec parity**：实现是否完整满足 spec 与 ticket acceptance criteria，有无功能缺失。**不满足即触发 hard gate**。
- **返工**：因选错 seam、漏掉调用方或按 finding 修改而重做的次数与原因（含"因 B-lite 误报导致返工"）。
- **D（bug？触发？）**：是否 bug 任务；若是，是否执行了根因搜索、调用方是否共享同一不变量、事后是否验证该判断正确。
- **实际收益（定性）**：与同类任务的比较——代码量、文件数、耗时、复杂度，填"更少/相当/更多 + 依据"。不填比例（5 个任务不支持量化）。

## 裁决（试点期满填写，逐条独立，不捆绑）

### Hard gate（触发即撤销对应条目，不等期满）

出现可归因于 A-lite / D 的任一项：

1. 安全回归；
2. 正确性回归；
3. **spec parity 缺失**（实现未满足 spec 或 acceptance criteria）；
4. **测试被省略或弱化**（以"少写代码"为由削减验证义务）；
5. **可访问性回归**；
6. 错误处理回归（尤其信任边界校验、防数据丢失处理）。

### 转正门槛（三条各自独立判定，每条需**同时满足**全部条件才转正）

1. **交付确认**：≥80% 的任务在"规则是否到达"栏有正向 evidence。未达标说明未验证的是送达而非规则本身——先解决送达再谈转正。
2. **A-lite**：至少一次实际改变实现选择，**且该次改变带来正向收益**（更少代码、更少文件、更少返工，且 spec parity 无损）。**仅改变选择不算**——改变可能更好，也可能更差。
3. **B-lite**：至少一个**独有**且被接受的 finding，且**误报未造成返工**。
4. **D**：至少一个 bug 任务实际触发，且"调用方是否共享同一不变量"的判断事后被验证为正确。
5. **样本**：≥5 个任务，且上述正向证据齐备。

### 未达门槛时的处置

- **有正向证据但不充分**（部分条件达标）：延长窗口，最多再 5 个任务；延长后仍不足则标记"未验证"。
- **无正向证据但未触发 hard gate**：标记"未验证"。
- **"未验证"的解读**：不构成采纳证据，也不构成否定证据——它表示本次试点没有能力回答该条是否有价值。
- **零 finding 的解读**：先查"规则是否到达"栏；未到达则不能作为规则的判断依据，已到达则说明代码本已精简或与 smell 基线重复。

### 期满裁决

对 A-lite / B-lite / D 各出一条结论——转正（保留并去掉"试点"标注）/ 延长后再裁 / 修订后再试 / 撤销 / 未验证。一条有效不得带动其余两条转正，一条无效也不否定其余两条。撤销只回退 `implementation-workflow.md` §6/§8 对应增补行，不株连其余两条。

裁决完成后：更新提案状态、同步 `docs/DOCS-INDEX.md`、删除 workflow §9 步骤 3，本文件移入 `docs/archive/reviews/`。
