# Binary media assets are stored as regular Git content.
#
# LFS tracking was removed 2026-09-05 (#196): the repo had drifted into a
# split state — older binaries committed raw while new ones went through
# LFS filters — which broke fresh checkouts and worktrees (pointer/diverged
# content, ~50 environment-class test failures). Total media is ~7MB with
# slow growth; LFS added toolchain failure modes for no storage benefit.
#
# Do NOT re-add filter=lfs patterns without an approved design: mixing
# filter state with already-committed raw content is what caused #196.
# SVG files are text-based (XML) and stay in regular Git for diffability.
